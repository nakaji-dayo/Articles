h2 index

h3 recently articles
- docs.forEach(function(doc){
	div.article
		h4
			a(href="/#{doc.url}") #{doc.title}
		dl.info
			dt reg date
			dd #{doc.reg_date}
			dt last update
			dd #{doc.update_date}
			dt tags
			dd
				ul
					- doc.tags.forEach(function(tag){
						li #{tag}
					- });
		div.contents
			!{doc.contents.replace(/<.*>/g,'').substring(0,500)+'...'}
-});
